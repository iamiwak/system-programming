<Window x:Class="myapp.Window1"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:myapp"
        mc:Ignorable="d"
        Title="Rename file" Height="150" Width="300">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <StackPanel Margin="0,20,0,0">
            <Label Content="Введите новое имя файла" Width="auto" FontSize="16" HorizontalAlignment="Center"/>
            <TextBox Width="150" HorizontalAlignment="Center"/>
            <StackPanel Margin="0,20,0,0" Orientation="Horizontal" HorizontalAlignment="Center">
                <Button Content="ОК" x:Name="okButton"/>
                <Button Content="Назад" x:Name="cancelButton"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</Window>
